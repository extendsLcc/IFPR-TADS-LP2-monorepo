/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Exercicio.Polimosfismo.exTeorico;

/**
 *
 * @author aluno
 */
public class ex {
    
    /*
    
    3)
    (a)Se a classe Alpha herda da classe Beta, a classe Alpha é chamada de ____sub___classe 
    e a classe Beta é chamada de ___super_ classe.
    
    (b)A herança permite ______a reutilização de codigo______, que economiza tempo no desenvolvimento e encoraja
    a utilização de componentes de software testados e de alta qualidade.
    
    c)Um objeto de uma _____sub_______ classe pode ser tratado como um objeto de sua______super________ 
    classe correspondente.
    
    (d)Os quatros especificadores de acesso de membro são ______private______, ______protected______,
    ______public______ e ______default_______.
    
    (e)Um relacionamento “tem um” entre as classes representa _______agregação________ e um relaci-onamento 
    “é um” entre classes representa ______composição________.
    
    (f)Se uma classe contém um ou mais métodos abstract, ela é uma classe ______abstrata________.
    
    4)
        herança simples: Quando uma subclasse herda os atributos,métodos e construtores de apenas uma superclasse.
        herança multipla: Quando uma subclasse herda os atributos,métodos e construtores de multiplas superclasses simultaneamente.
        interface: A interface define uma lista de metodos que toda classe que implementar a interface, obrigatóriamente deve implementa-las.
        superclasse: classe que é extendida por uma subclasse.
        subclasse: classe que extende uma superclasse, e herda seus metodos, atributos e construtores.
    
    5)
        Herança multipla herda multiplas superclasses simultaneamente enquanto herança simples herda apenas uma superclasse.
        Em java, apenas com interface é possivel utilizar herança multipla.
    
    6)
        true
    
    7)
        true
    
    8)
        protected: pode ser utilizado em casos que um método ou atributo necessite ser acessado do mesmo pacote ou alguma subclasse
        que extende a classe com o acesso protected.
    
    9)
        Permitindo que objetos instanciados de uma subclasse sejam tratados como a superclasse dessas subclasses.
        Isso permite que todos os metodos em comum entre as subclasses herdadas da superclasse podem ser acessadas.
    
    10)
        Métodos abstract não são concretos e obrigatóriamente devem ser implementados na subclasse que herda a superclasse que possui o método abstrato.
    
    11)
        false
    
    13)
        A classe Produto não é abstrata, portanto não pode declarar métodos abstratos.
    
    14)
        Não é possivel declarar atributos abstratos.
    
    15)
        Não é possivel declarar o corpo do método abstrato toString
    
    16)
        Não é possivel declarar métodos de mesma assinatura dentro da classe, o atributo valor é declarado como final
        então caso for chamado o metodo reseta mais de uma vez, ocorrera um erro.
    
    17)
        Em java não é possivel extender mais de uma classe, o constructors e métodos das superclasses esta sendo chamado incorretamente
        deve ser chamado por super.
    
    18)
        O método do construtor pai deve ser o primeira instrução do construtor do metodo filho.
        
        
    
    */
    
}
